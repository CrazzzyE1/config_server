server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CrazzzyE1/mystore-config.git
          search-paths: first-test
          clone-on-start: true
  application:
    name: config_server

logging:
  level:
    org.springframework.cloud: DEBUG
